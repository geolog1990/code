using System;

namespace CycleFor
{
    /*
     * Цикл for 
     */
    class Program
    {
        static void Main(string[] args)
        {
            /*int limit = int.Parse(Console.ReadLine());
            
            for (int i = 0; i <= limit; i++)
            {
                Console.WriteLine(i);
            }
            */

            for (int i = 5; i > 0; i--)
            {
                Console.WriteLine(i);
            }
        }
    }
}
